require(['app', 'jQuery', 'backbone', 'jQuery-ui'], function (app, $, Backbone) {
    'use strict';
    app.initialize();
    // console.log(app);
    // console.log('Running jQuery %s', $().jquery);
});